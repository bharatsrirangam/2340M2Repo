defaultTasks 'jar'

apply plugin 'java'


dependencies {
	
}

repositories {
	mavenCentral()
}

jar { 
	manifest {
		attributes '<attribute>' : '______', <attribute>: '_______'
	}

	destinationDir = file('.')
	version = '1.0'
}